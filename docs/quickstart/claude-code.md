# Quickstart: Claude Code + Munin

1. Export:
   - `MUNIN_BASE_URL`
   - `MUNIN_API_KEY`
   - `MUNIN_PROJECT`
2. Wire adapter `@munin/adapter-claude-code`.
3. Call `execute(action, payload)` with core actions.
4. Run smoke checks and contract suite.
