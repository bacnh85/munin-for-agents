# Quickstart: Cursor + Munin

1. Set env vars:
   - `MUNIN_BASE_URL`
   - `MUNIN_API_KEY`
   - `MUNIN_PROJECT`
2. Install dependencies in `munin-ecosystem`.
3. Use `@munin/adapter-cursor` to invoke core actions.
4. Verify with contract test:
   - `pnpm test:contract`
