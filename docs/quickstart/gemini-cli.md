# Quickstart: Gemini CLI + Munin

1. Configure env vars:
   - `MUNIN_API_KEY`
   - `MUNIN_PROJECT`
2. Initialize `@kalera/munin-gemini`.
3. Call tools via `callTool(name, args)`.
4. Validate behavior with contract tests.
